name: sunrai-rag
channels: [conda-forge, defaults]
dependencies:
  - python=3.11
  - pip=24.0
  - tesseract=5.4.1          # OCR binary, installed by conda so no apt needed
  - pip:
      - -r requirements.txt
